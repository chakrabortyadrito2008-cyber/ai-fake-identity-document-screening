# Model manifests

Example fields: `name`, `version`, `sha256`, `source`, `license`, `input`, `output_semantics`, and `path`. Never activate an unverified model. The configured YuNet model has no manifest/checkpoint in this repository; it is intentionally unavailable until the organisation supplies a licence-approved file and its SHA-256.
